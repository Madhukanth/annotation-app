/**
 * Types Module Index
 * Central export point for all TypeScript types
 *
 * Usage:
 *   import { Project, Shape, Annotation } from '@/types'
 *   import { Rectangle, Circle } from '@/types/shapes'
 *   import { File, Comment } from '@/types/entities'
 */

// Entity types
export * from './entities'

// Shape types
export * from './shapes'

// API/Media types
export * from './api'

// UI types
export * from './ui.types'
